#include <stdio.h>
/**
 * main - entry point
 *
 * Description: print single digit numbers of base 10 starting from
 *
 * Return: 0
 */
int main(void)
{
	int digit;

	for (digit = 0 ; digit <= 9 ; digit++)
		printf("%i", digit);
	return (0);
}
