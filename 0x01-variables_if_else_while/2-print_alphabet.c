#include <stdio.h>
/**
 * main - main block
 * Description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */
int()
{
        char = 'a';
        while (ch <= 'z')
        {
                putchar('ch');
                ch++;

        }
        putchar('\n');

        return (0);


}

