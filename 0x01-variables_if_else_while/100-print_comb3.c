#include <stdio.h>
/**
 * main - entry point
 *
 * Description: print lowwer and upper alphabet
 *
 * Return: 0
 */
int main(void)
{
	int i, j;
	for (i = 0;i <=9; i++)
	{
		for (j = 0 ;j <= 9;j++)
			if(i == j)
				i++;
		printf("%i%i,", ij);
	return (0);
	}
}
